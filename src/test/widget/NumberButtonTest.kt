import kotlin.test.assertEquals
import org.junit.Test;
import widget.Button
import widget.NumberField
import timeline.EventFieldType
import timeline.IntValue
import widget.Pos
import timeline.AppState


Test
private fun testButtonDown() {
	val value = IntValue(2)
	/*val state = AppState()
	val
	NumberField(value, 10, Pos(10, 10), {
		//valueLabels = EventFieldType.values().map { it.toString() }.copyToArray()
	}).handleEvents()*/

	assertEquals("Hello world!", "hello world!".capitalize())
}